name: build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      matrix:
        python-version: ['3.9', '3.13']
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package + test extras
        run: pip install -e ".[test]"

      - name: Import smoke
        run: python -c "import modules.main, checks, datasources"

      - name: Type check
        run: python -m mypy --ignore-missing-imports --follow-imports=silent checks/core/

      - name: Run framework tests
        run: >-
          pytest tests/ -v --maxfail=5
          -m "not neo4j and not integration"
          --cov=modules --cov=checks --cov=datasources
          --cov-report=xml --cov-report=term

      - name: Upload coverage
        if: matrix.python-version == '3.13'
        uses: codecov/codecov-action@v6
        with:
          files: coverage.xml
          fail_ci_if_error: false

  neo4j-fixture-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      neo4j:
        image: neo4j:5-community
        env:
          NEO4J_AUTH: neo4j/testpassword
          NEO4J_PLUGINS: '[]'
        ports:
          - 7687:7687
        options: >-
          --health-cmd "cypher-shell -u neo4j -p testpassword 'RETURN 1'"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install package + test extras
        run: pip install -e ".[test]"

      - name: Run Neo4j fixture tests
        env:
          ADPF_TEST_ALLOW_WIPE: '1'
          NEO4J_URI: bolt://localhost:7687
          NEO4J_USER: neo4j
          NEO4J_PASSWORD: testpassword
        run: pytest tests/ -v --maxfail=5 -m "neo4j and not integration" --no-cov
